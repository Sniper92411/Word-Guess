# 版本：v1.7

# 这是猜词游戏的配置文件，以井号开头的是注释，提供提示说明，修改无效
# 每一个设置都由一个设置名和一个值组成，以斜杠分隔

# 如果需要更改设置: 只允许修改=后的值，否则会出现错误导致无法运行程序

# 每一条用于提示设置功能的注释会显示为如下格式
# 设置名称(默认值): 功能

# 如果在调整设置后无法正常启动程序，请重新下载该设置文件
# 下载地址: https://github.com/Sniper2711/Word-Guess/releases


# Flash(1): 控制猜测/猜词/展示评级时是否闪烁屏幕
Flash=1

# Language(zh_cn): 选择程序的显示语言，当前支持的：zh_cn、en_us
Language=zh_cn

# Debug(0): 调试模式，仅供开发者使用，开启后能够实时监控变量和程序状态
Debug=0

# CustomInputFile(words.txt): 控制程序从哪个Text File输入单词，可用于自定义词库（格式参考默认词库words.txt）
CustomInputFile=words.txt

# DictionaryMode(0): 启用新的模式：词典模式，用于需要批量记忆单词的用户练习记忆，设置为1启用
DictionaryMode=0
